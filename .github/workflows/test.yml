# name: Test

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   vitest:
#     name: Vitest (Bun ${{ matrix.bun }} / Node ${{ matrix.node }})
#     runs-on: ubuntu-latest
#     timeout-minutes: 15
#     strategy:
#       fail-fast: false
#       matrix:
#         node: ["20", "22"]
#         bun: ["1.1.38", "latest"]
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node.js ${{ matrix.node }}
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ matrix.node }}
#           cache: "npm"

#       - name: Setup Bun ${{ matrix.bun }}
#         uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: ${{ matrix.bun }}

#       # Cache Bun's global install/cache directory across runs.
#       - name: Cache Bun global store
#         uses: actions/cache@v4
#         with:
#           path: |
#             ~/.bun/install/cache
#           key: ${{ runner.os }}-bun-${{ matrix.bun }}-${{ hashFiles('**/bun.lockb', '**/package.json') }}
#           restore-keys: |
#             ${{ runner.os }}-bun-${{ matrix.bun }}-

#       # Cache the project's node_modules so reinstalls are near-instant.
#       - name: Cache node_modules
#         uses: actions/cache@v4
#         with:
#           path: node_modules
#           key: ${{ runner.os }}-node${{ matrix.node }}-bun${{ matrix.bun }}-modules-${{ hashFiles('**/bun.lockb') }}
#           restore-keys: |
#             ${{ runner.os }}-node${{ matrix.node }}-bun${{ matrix.bun }}-modules-

#       - name: Install dependencies
#         run: bun install --frozen-lockfile

#       - name: Run full vitest suite (includes internal link check)
#         run: bun run test

#       - name: Validate SEO
#         run: bun run validate:seo


name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install
      - run: npm run test